module.exports = {
    run: function(creep) {
        // Defense logic here.

        // Find hostiles in room

        // Attack if in range
        // Move to if not in range
        console.log("Defender ready, but not implemented.")
    }
}